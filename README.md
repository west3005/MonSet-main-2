# MonSet

> **Ветка активной разработки** MonSet до продаваемой версии

***

## О проекте

MonSet — промышленное IoT-устройство для мониторинга технологических параметров на объектах.

***

## Аппаратная платформа

| Компонент | Описание |
|---|---|
| **MCU** | STM32F407VET6 на плате STM32F4VE (168 MHz, 512 KB Flash, 192 KB RAM) |
| **Ethernet** | W5500 (SPI1) |
| **GSM / NB-IoT** | SIM7020C → SIM7020E (Cat1-A7670X-V1.02) |
| **WiFi** | ESP8266 (USART6) |
| **RTC** | DS3231 (I2C1) |
| **Датчики** | Modbus RTU: (USART3 RS-485) унивесальный, настраивается в конфиге, (UART4 RS-485)унивесальный, настраивается в конфиге |
| **Хранение** | Встроенная MicroSD в плату STM32F4VE (FAT32, SDIO 1-bit - инициализация, потом переход на 4-bit - работа) |
| **Спутник** | Iridium SBDNR9602G (UART5 RS-232/485) унивесальный, настраивается в конфиге. Аппаратно разведен на 2 линии MAX485 и MAX3232 |

### Схема питания

```
LiFePo4 24V 15Ah
    │
    ├─ 24V ──────────────── Датчики (415 погружной, ULM31 радарный)
    │
    ├─ DC-DC 24V → 5V ───── ESP8266, W5500, DS3231, GSM-модем
    │                       (далее LDO 5V → 3.3V для каждого модуля)
    │
    └─ DC-DC 5V → 3.3V ──── STM32F407VET6
```

***

## Функционал устройства

### Сбор данных

1. Считывает данные с датчиков по Modbus RTU (UART/RS-485)
2. Синхронизирует время через NTP или DS3231
3. Логирует данные на SD-карту с ротацией по дате
4. Усреднение за выбранное количество измерений
5. Все опросы датчиков сохраняются на SD в файл `Backup.json`
6. В конфиге — настройка нулевой отметки (уровень моря), коэффициентов умножения/деления для конвертации

### Передача данных

7. Отправляет телеметрию на ThingsBoard через GSM и Ethernet W5500 (HTTPS и MQTT — выбор через конфиг)
8. Поддержка дополнительных MQTT-брокеров: Home Assistant (Mosquitto), Node-RED, Yandex IoT Core, свой сервер
9. HTTP Webhook — отправка на произвольный URL (Zapier, IFTTT, собственный сервер) по событию или расписанию
10. **Приоритетная цепочка каналов** Данный режим включается/отключается и настраивается в конфиге:
    - Ethernet W5500 → GSM  → WiFi ESP8266 - Iridium SBD
    - Автопереключение при недоступности, возврат на приоритетный при восстановлении
11. WatchDog на каждый канал связи — GSM, Ethernet, WiFi, IRIDIUM
12. При неудачной отправке данные сохраняются в `Backup.json` и отправляются после восстановления связи
13. После удачной отправки буферные данные стираются
14. Отдельная дискретность отправки `Backup.json` на сервер (независима от дискретности опроса)

### Конфигурация

15. Конфиг хранится на SD-карте (`config.json`)
16. Удалённое конфигурирование через WiFi ESP8266, по Ethernet и по GSM
17. Конфигурируемая карта регистров Modbus в JSON на SD: адрес, функция, тип данных (`int16`/`uint32`/`float`), масштабирующий коэффициент, единица измерения — без перепрошивки
18. Настройка каждого порта отдельно: параметры порта, параметры Modbus, несколько датчиков с разными адресами на одном канале
19. Поддержка Modbus TCP через W5500 — опрос устройств в локальной сети
20. Так же сам может выступать к качестве Modbus TCP slave и передавать измерения.

### Веб-интерфейс

20. Веб-интерфейс через ESP8266, GSM и Ethernet (W5500) — с Runtime-логами и конфигом
21. Весь конфиг настраивается через вэб интефейс
22. Адаптивный интерфейс с окнами для отдельных категорий
23. Отображение последних значений датчиков без необходимости ThingsBoard
24. Ведение лога на отдельной вкладке
25. Скачивание логов через веб-интерфейс
26. Экспорт данных из `Backup.json` в CSV (для Excel / Google Sheets)
27. Индикация уровня заряда батареи через ADC с отправкой на сервер и предупреждением при низком заряде
28. Включение и отключение каналов измерения 
29. **Мастер первоначальной настройки** (Captive Portal через ESP8266):
    - *Простая версия* — подключение к WiFi, APN, MQTT-брокер, токен ThingsBoard, сервер
    - *Расширенная версия для разработчика* — все возможные настройки
    

### Безопасность

28. Безопасное удалённое управление с логином и паролем
29. TLS + веб-интерфейс с авторизацией
30. *(Опция, на будущее)* Mutual TLS (mTLS) — двусторонняя аутентификация, хранение ключей во внутренней Flash STM32

### Надёжность

31. WatchDog для всех функций программы и автовосстановления (IWDG ~32 с)
32. *(Опция, на будущее)* Поддержка Modbus TCP Server — устройство как Modbus-slave для SCADA (GE iFIX, WinCC, InTouch)

***

## Конечный автомат App

```
INIT → ETHERNET_INIT → NTP_SYNC → MODEM_INIT → SENSORS_POLL → SEND_DATA → SLEEP
                                                      ↑                        │
                                                      └────────────────────────┘
```

***

## Аппаратная архитектура — назначение портов и пинов

> **Источник истины по пинам:** `Core/Inc/board_pins.hpp`  
> **Конфигурация периферии:** `MonSet.ioc` (STM32CubeMX)  
> **Компиляционные константы:** `Core/Inc/config.hpp`

### MCU и тактирование

| Параметр | Значение |
|---|---|
| MCU | STM32F407VET6 (LQFP100) |
| Частота ядра | 168 MHz (PLL от HSI 16 MHz: PLLM=16, PLLN=336, PLLP=DIV4) |
| Flash | 512 KB |
| RAM | 192 KB |
| APB1 | 84 MHz |
| APB2 | 168 MHz |
| RTC clock | LSE 32.768 kHz |
| Debug/Prog | SWD — PA13 (SWDIO), PA14 (SWCLK) |

***

### UART / USART — назначение портов

| Интерфейс | TX / RX | Скорость | Назначение | HAL-хэндл |
|---|---|---|---|---|
| **USART1** | PA9 / PA10 | 115200 8N1  порт настраивается в вэб интрфейсе| Отладочный UART (CH340, USB-UART) После отладки используется как универсальный датчик с настройкой в вэб интерфейсе | `huart1` |
| **USART2** | PA2 / PA3 | 115200 8N1 | GSM-модем (SIM7020C / SIM7020E / Air780E) фиксированный | `huart2` |
| **USART3** | PB10 / PB11 | порт настраивается в вэб интрфейсе | RS-485 Modbus RTU — универсальный датчик с настройкой в вэб интерфейсе| `huart3` |
| **UART4** | PA0 / PA1 | порт настраивается в вэб интрфейсе | RS-485 Modbus RTU — универсальный датчик с настройкой в вэб интерфейсе | `huart4` |
| **UART5** | — |  порт настраивается в вэб интрфейсе | универсальный аппаратно. RS-485 Modbus RTU / RS-232 через MAX3232 —  универсальный датчик с настройкой в вэб интерфейс | — |
| **USART6** | PC6 / PC7 | 115200 8N1 | ESP8266 (WiFi) + зеркало Debug фиксированный| `huart6` |

> **UART5 (Iridium):** в текущей реализации управление ведётся через GPIO (EDGE_ON / EDGE_NET_AVAIL / EDGE_PWR_DET). UART5 по выбору под RS-232 через MAX3232. Аппаратно расходится на MAX3232 и MAX485 

#### RS-485 Direction Control

| Сигнал | Пин | Тип | Назначение |
|---|---|---|---|
| `RS485_DE` | **PB12** | Output PP | DE/RE RS-485 трансивера (HIGH = передача, LOW = приём) |

***

### SPI1 — W5500 Ethernet

| Сигнал | Пин | Назначение |
|---|---|---|
| SCK | **PA5** | SPI1 Clock |
| MISO | **PA6** | SPI1 Master In |
| MOSI | **PA7** | SPI1 Master Out |
| `W5500_CS` | **PA8** | Chip Select (Output, active LOW) |
| `W5500_RST` | **PC3** | Hardware Reset (Output, active LOW) |
| `W5500_INT` | **PC4** | Interrupt от W5500 (Input) |

> Скорость SPI1: Prescaler = 64 → **1.3125 MBits/s**  
> MAC по умолчанию: `02:30:05:00:00:01` (задаётся в `config.hpp`)

***

### SDIO — MicroSD карта (встроена в плату STM32F4VE)

| Сигнал | Пин | Назначение |
|---|---|---|
| `SDIO_CK` | **PC12** | Clock |
| `SDIO_CMD` | **PD2** | Command |
| `SDIO_D0` | **PC8** | Data 0 (1-bit mode) |

> Режим: **1-bit SDIO**, файловая система FAT32 через FatFS  
> ClockDiv = 2 → эффективная тактовая SD ≈ 6 MHz  
> При сбросе по IWDG/WWDG/Software — SD отключается на текущий запуск (`g_sd_disabled = true`)

***

### I2C1 — DS3231 RTC

| Сигнал | Пин | Назначение |
|---|---|---|
| SCL | **PB6** | I2C1 Clock |
| SDA | **PB7** | I2C1 Data |

> DS3231 I2C адрес: `0x68` (7-bit) / `0xD0` (8-bit write)  
> Скорость: 100 kHz (Standard Mode)

***

### GPIO — управление GSM-модемом

| Сигнал | Пин | Тип | Назначение |
|---|---|---|---|
| `CELL_PWR_EN` | **PC2** | Output PP | Включение питания GSM (HIGH = вкл) |
| `CELL_PWRKEY_N` | **PD6** | Output OD, active LOW | PWRKEY — импульс к GND через транзистор |
| `CELL_RESET_N` | **PD7** | Output OD, active LOW | Hardware RESET через транзистор |
| `CELL_RI_WAKE` | **PD8** | Input | Ring Indicator / Wake-up от модема |
| `CELL_STATUS` | **PD9** | Input | Статус питания модема (HIGH = готов) |
| `CELL_DTR` | **PD10** | Output PP | DTR — управление sleep-режимом модема |

***

### GPIO — управление Iridium Edge

| Сигнал | Пин | Тип | Назначение |
|---|---|---|---|
| `EDGE_ON` | **PD3** | Output OD | Включение блока Iridium (Open-Drain) |
| `EDGE_NET_AVAIL` | **PD4** | Input | Наличие спутниковой сети |
| `EDGE_PWR_DET` | **PD5** | Input | Детектирование питания блока (после MAX3232) |

***

### GPIO — управление ESP8266

| Сигнал | Пин | Тип | Назначение |
|---|---|---|---|
| `ESP_EN` | **PE0** | Output PP | Chip Enable ESP8266 (default LOW = выкл) |
| `ESP_RST` | **PE1** | Output PP | Hardware RESET (active LOW) |
| `ESP_GPIO0` | **PE2** | Output PP | Boot mode: HIGH = normal, LOW = flash mode |

***

### GPIO — системные

| Сигнал | Пин | Тип | Назначение |
|---|---|---|---|
| `STATUS_LED` | **PC1** | Output PP | Индикаторный светодиод статуса |
| `Led` (board) | **PC13** | Output PP | Встроенный LED платы STM32F4VE |
| `MODE_SWITCH` | **PC0** | Input Pull-UP | Кнопка/переключатель режима (LOW = нажато) |
| `NET_SELECT` | **PB0** | Input Pull-UP | Выбор сети ETH/GSM аппаратно |
| `GPIO_RESERVED1` | **PD11** | Input | Зарезервирован |
| `GPIO_RESERVED2` | **PD12** | Input | Зарезервирован |

***

### Таймеры и системные периферии

| Периферия | Конфигурация | Назначение |
|---|---|---|
| **TIM6** | Prescaler=83, Period=999 → тик 1 мс | Таймаут Modbus RTU (межфреймовая пауза T3.5) |
| **RTC** | LSE 32.768 kHz, WakeUp каждые 60 с | Хранение времени, пробуждение из STOP-режима |
| **IWDG** | LSI, Prescaler=256, RLR=4095 → ~32 с | Аппаратный WatchDog — сброс при зависании |
| **RNG** | Аппаратный генератор STM32 | Энтропия для mbedTLS (TLS-хендшейк) |
| **SysTick** | HAL tick 1 мс | `HAL_Delay`, таймауты HAL |
| **DMA2** | Включён для SDIO | Буферизованный обмен с SD-картой |

***

### Используемые библиотеки и Middleware

| Библиотека | Расположение | Назначение |
|---|---|---|
| STM32Cube HAL F4 v1.28.0 | `Drivers/` | HAL-уровень STM32 |
| FatFS | `FATFS/` | Файловая система SD-карты |
| mbedTLS | `Middlewares/Third_Party/` | TLS 1.2 для HTTPS (W5500 + GSM) |
| W5500 ioLibrary | `Drivers/` | Ethernet TCP/IP стек |

***

### Файловая структура SD-карты

| Файл | Назначение |
|---|---|
| `config.json` | Runtime-конфигурация (APN, сервер, интервалы, карта Modbus) |
| `backup.jsn` | Буфер телеметрии при потере связи (JSONL, строки до 240 байт) |
| `log_YYYY-MM-DD.txt` | Ротируемые лог-файлы по дате |

***

### Зарезервированные ресурсы для будущей разработки

| Ресурс | Пин / Интерфейс | Планируемое использование |
|---|---|---|
| `GPIO_RESERVED1` | PD11 | Дискретный вход / счётный вход (4–20 мА) |
| `GPIO_RESERVED2` | PD12 | Дискретный вход / управление внешним реле |
| UART5 / RS-232 | MAX3232 | Iridium SBDNR9602G полноценный UART |
| Flash Sector 6–7 | 0x08060000 | TLS-ключи (mTLS) / NVS |
| I2C1 (PB6/PB7) | — | Расширение: BME280 (T/H/P), PCF8574 (дискреты) |

***

## Требования и конфигурация

### config.json на SD-карте

Все изменяемые параметры хранятся в `config.json` на SD-карте и применяются без перепрошивки:

- APN, логин/пароль оператора
- URL сервера, токен авторизации, протокол (HTTPS / MQTT)
- Дискретность опроса датчиков и дискретность отправки
- Приоритеты каналов связи
- Карта регистров Modbus для каждого порта
- Нулевая отметка и коэффициенты масштабирования

***

## Выбор GSM-модуля

> Текущий модуль: **SIM7020C**

### 🥇 SIM7020E — оптимальный выбор

Прямая pin-to-pin и AT-команды совместимая замена SIM7020C.
Добавляет диапазоны **B20** и **B28** — полное покрытие МТС, МегаФон, Билайн, Теле2 в России.
Код драйвера MonSet переносится без изменений: те же `AT+CMQTTCONNECT`, `AT+CMQTTPUB`.
Потребление в PSM ~1 µA — идеально для батарейного питания.

### 🥈 Quectel BC660K-GL — лучший NB-IoT чип

NB-IoT Cat NB2 (3GPP Rel.14).
Поддерживает B1/B2/B3/B5/B8/B12/B13/B14/B17/B18/B19/**B20**/B25/B28/B66/B70/B85 — самый широкий охват.
Встроенный eSIM, потребление в активном режиме ~5 mA.
Требует нового драйвера под Quectel AT-команды.

### 🥉 Quectel BG95-M3 — лучший для отдалённых объектов

Единственный с **2G EGPRS fallback** — при отсутствии NB-IoT автоматически переключается на GPRS.
Поддержка Cat-M1 даёт скорость до 588 kbps для FOTA-обновлений.
Продаётся в России через Aurora Evernet.
Потребление ~18 mA в активном режиме — оправдано для объектов без NB-IoT покрытия.

***

## Лицензия

***

*Этот репозиторий — рабочая ветка разработки. Стабильная версия: [west3005/MonSet](https://github.com/west3005/MonSet)*
